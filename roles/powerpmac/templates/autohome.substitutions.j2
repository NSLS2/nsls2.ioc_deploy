#
# {{ ansible_managed }}
#
file "${PPMAC}/db/autohome.db"
{{"{"}}
pattern
{{"{"}}SYS, DEV, PLC, PORT{{"}"}}
{% for axis in ioc.axes %}
{{"{"}}"{{ ioc.environment.SYS }}", "{{"{"}}{{ axis.component }}-Ax:{{ axis.name }}{{"}"}}", {{ axis.home_plc }}, "$(SSH_PORT)"{{"}"}}
{% endfor %}
{% if ioc.virtual_axes is defined %}
{% for axis in ioc.virtual_axes %}
{{"{"}}"{{ ioc.environment.SYS }}", "{{"{"}}{{ axis.component }}-Ax:{{ axis.name }}{{"}"}}", {{ axis.home_plc }}, "$(SSH_PORT)"{{"}"}}
{% endfor %}
{% endif %}
{{"}"}}
